# Path to the manually-placed model_factory folder (defaults to ./model_factory)
MODEL_FACTORY_DIR=./model_factory

# Comma-separated list of allowed CORS origins, or "*" for all (default)
CORS_ORIGINS=*
